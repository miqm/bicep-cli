---
# See ../DEPENDENCIES.md#workaround-for-other-dependencies
name: Dependabot hack
on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - never-trigger-this-dependabot-hack-workflow

jobs:

  dependabot_hack:
    name: Ensure dependabot version checks
    runs-on: ubuntu-20.04
    steps:    
      # Dockerfile:
      - uses: azure/bicep@v0.18.4
      - uses: azure/azure-cli@azure-cli-2.50.0
      - uses: mikefarah/yq@v4.34.2
      - uses: geofffranks/spruce@v1.30.2
      - uses: Azure/azure-storage-azcopy@v10.19.0
      - uses: tfutils/tfenv@v3.0.0
      - uses: kubernetes/kubernetes@v1.26.7
      - uses: helm/helm@v3.12.2
      - uses: Azure/kubelogin@v0.0.30
      # ci/bicep-cli.yml:
      - uses: moby/moby@v20.10.24
