---
# See ../DEPENDENCIES.md#workaround-for-other-dependencies
name: Dependabot hack
on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - never-trigger-this-dependabot-hack-workflow

jobs:

  dependabot_hack:
    name: Ensure dependabot version checks
    runs-on: ubuntu-20.04
    steps:    
      # Dockerfile:
      - uses: azure/bicep@v0.5.6
      - uses: azure/azure-cli@azure-cli-2.36.0
      - uses: mikefarah/yq@v4.25.1
      - uses: geofffranks/spruce@v1.29.0
      - uses: Azure/azure-storage-azcopy@v10.14.1
      - uses: tfutils/tfenv@v2.2.3
      # ci/bicep-cli.yml:
      - uses: moby/moby@v20.10.13
