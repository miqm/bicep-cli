---
# See ../DEPENDENCIES.md#workaround-for-other-dependencies
name: Dependabot hack
on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - never-trigger-this-dependabot-hack-workflow

jobs:

  dependabot_hack:
    name: Ensure dependabot version checks
    runs-on: ubuntu-20.04
    steps:    
      # Dockerfile:
      - uses: azure/bicep@v0.13.1
      - uses: azure/azure-cli@azure-cli-2.45.0
      - uses: mikefarah/yq@v4.31.2
      - uses: geofffranks/spruce@v1.30.0
      - uses: Azure/azure-storage-azcopy@v10.18.0
      - uses: tfutils/tfenv@v3.0.0
      - uses: kubernetes/kubernetes@v1.26.2
      - uses: helm/helm@v3.11.2
      - uses: Azure/kubelogin@v0.0.27
      # ci/bicep-cli.yml:
      - uses: moby/moby@v20.10.23
