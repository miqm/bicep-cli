---
# See ../DEPENDENCIES.md#workaround-for-other-dependencies
name: Dependabot hack
on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - never-trigger-this-dependabot-hack-workflow

jobs:

  dependabot_hack:
    name: Ensure dependabot version checks
    runs-on: ubuntu-20.04
    steps:    
      # Dockerfile:
      - uses: azure/bicep@v0.9.1
      - uses: azure/azure-cli@azure-cli-2.40.0
      - uses: mikefarah/yq@v4.27.2
      - uses: geofffranks/spruce@v1.29.0
      - uses: Azure/azure-storage-azcopy@v10.16.0
      - uses: tfutils/tfenv@v3.0.0
      # ci/bicep-cli.yml:
      - uses: moby/moby@v20.10.17
